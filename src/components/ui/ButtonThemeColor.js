import React from 'react'

const ButtonThemeColor = ({buttontext}) => {
  return (
    <button className="button-theme-color">
        {buttontext}
    </button>
  )
}

export default ButtonThemeColor